% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FuncMetaWorkflow.R
\name{runMetaWorkflow}
\alias{runMetaWorkflow}
\title{Run the Full Meta Workflow for One Drug and Tumor Type}
\usage{
runMetaWorkflow(
  drug,
  tumor_type,
  feature2_type = "mRNA",
  data_type = "all",
  cores = 3,
  cell_min_intersected_cells = 20,
  pdcpdx_min_intersected_cells = 8,
  cell_es_t = 0.1,
  cell_P_t = 0.05,
  cell_n_datasets_t = 3,
  pdcpdx_es_t = 0.1,
  pdcpdx_P_t = 0.05,
  pdcpdx_n_datasets_t = 2,
  tcga_ad_p_t = 0.01,
  clinical_es_t = 0.1,
  clinical_P_t = 0.05,
  clinical_n_datasets_t = NULL,
  db_path = getMetaWorkflowDefaults()$droma_db_path,
  ctrdb_path = getMetaWorkflowDefaults()$ctrdb_path,
  tcga_rna_counts_dir = getMetaWorkflowDefaults()$tcga_rna_counts_dir,
  gene_probe_map_path = getMetaWorkflowDefaults()$gene_probe_map_path,
  output_base = getMetaWorkflowDefaults()$output_base,
  verbose = TRUE
)
}
\arguments{
\item{drug}{Drug name.}

\item{tumor_type}{Tumor type.}

\item{feature2_type}{Molecular feature type, typically `"mRNA"`.}

\item{data_type}{Data type forwarded into DROMA workflow calls.}

\item{cores}{Number of cores used by downstream functions.}

\item{cell_min_intersected_cells}{Minimum intersected cells threshold for cell/PDC.}

\item{pdcpdx_min_intersected_cells}{Minimum intersected cells threshold for PDO/PDX.}

\item{cell_es_t}{Cell-line effect-size threshold.}

\item{cell_P_t}{Cell-line p-value threshold.}

\item{cell_n_datasets_t}{Cell-line dataset-count threshold.}

\item{pdcpdx_es_t}{PDO/PDX effect-size threshold.}

\item{pdcpdx_P_t}{PDO/PDX p-value threshold.}

\item{pdcpdx_n_datasets_t}{PDO/PDX dataset-count threshold.}

\item{tcga_ad_p_t}{Anderson-Darling concordance threshold.}

\item{clinical_es_t}{Clinical effect-size threshold.}

\item{clinical_P_t}{Clinical p-value threshold.}

\item{clinical_n_datasets_t}{Clinical dataset-count threshold.}

\item{db_path}{Path to the DROMA SQLite database.}

\item{ctrdb_path}{Path to the CTRDB SQLite database.}

\item{tcga_rna_counts_dir}{Directory containing TCGA/TARGET counts.}

\item{gene_probe_map_path}{Path to the probe-map file.}

\item{output_base}{Base directory for workflow outputs.}

\item{verbose}{Logical, whether to print progress messages.}
}
\value{
A one-row `data.table` summarizing workflow status and counts.
}
\description{
Executes the four workflow stages for a single `drug` and
`tumor_type` pair, writes intermediate outputs, and returns a one-row summary.
}
